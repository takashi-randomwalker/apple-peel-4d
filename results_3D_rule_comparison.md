# 3次元 Apple-Peel ペーリング：規則2 vs 規則3 比較実験

**実施日：** 2026-04-03
**実装ファイル：** `peeling3DLoxo.m`

---

## 1. 目的

3次元多面体に対する Apple-Peel ペーリングにおいて，
§4（`summary.tex`）で議論した二つの選択規則の性能を比較する．

- **規則2（loxodrome）**：左半空間候補の中から前進方向との角度 $\varphi_j$ が最小のものを選ぶ（球面上の等角螺旋近似）
- **規則3（max z / 緯度優先）**：左半空間候補の中から $z$ 座標が最大のものを選ぶ（Kaino 2019 に対応）

---

## 2. アルゴリズムの説明

### 2.1 共通の前処理

1. 頂点座標の重心を原点に移動する
2. 出発面 $C_1$ の重心が $+z$ 軸方向に来るよう SO(3) 回転を適用する
3. 全面の重心 $\mathbf{c}_i \in \mathbb{R}^3$ を計算する
4. 辺共有（頂点2個以上を共有）に基づく隣接リストを構築する

### 2.2 左半空間条件（共通）

各ステップ $k \geq 1$ において，直前の面 $C_{k-1}$ と現在の面 $C_k$ の重心，
および候補面 $j$ の重心に対し，

$$\det\!\bigl(\mathbf{c}_{k-1},\, \mathbf{c}_k,\, \mathbf{c}_j\bigr) \;\ge\; 0$$

を満たす候補を左半空間候補 $\mathcal{L}_k$ とする（3成分行列式条件，定義4.1の3次元版）．

> **注：** これは Darboux フレームの左方向 $\hat{L}_k = \hat{n}_k \times \hat{f}_k$ を用いた条件
> $\hat{L}_k \cdot \mathbf{c}_j \geq 0$ と完全に等価である（`summary.tex` §4.1 参照）．

### 2.3 Darboux フレーム

$$\hat{n}_k = \frac{\mathbf{c}_k}{|\mathbf{c}_k|}, \quad
\hat{f}_k = \frac{\mathbf{c}_k - \mathbf{c}_{k-1} - \bigl((\mathbf{c}_k-\mathbf{c}_{k-1})\cdot\hat{n}_k\bigr)\hat{n}_k}
                 {|\cdots|}, \quad
\hat{L}_k = \hat{n}_k \times \hat{f}_k$$

候補 $j$ の前進方向からの角度（接平面内）：

$$\varphi_j = \mathrm{atan2}\!\left(\tilde{\mathbf{c}}_j \cdot \hat{L}_k,\; \tilde{\mathbf{c}}_j \cdot \hat{f}_k\right)
\in (-\pi, \pi], \quad \tilde{\mathbf{c}}_j = \frac{\mathbf{c}_j - (\mathbf{c}_j \cdot \hat{n}_k)\hat{n}_k}{|\cdots|}$$

左半空間は $\varphi_j \in [0, \pi]$，すなわち $\tilde{\mathbf{c}}_j \cdot \hat{L}_k \geq 0$ に相当する．

### 2.4 選択規則

| 規則 | 選択基準（左候補あり） | フォールバック（左候補なし） |
|------|----------------------|---------------------------|
| 規則2（loxodrome）| $\arg\min_{j \in \mathcal{L}_k} \varphi_j$（最小角度） | $\arg\min_{j} |\varphi_j|$（最も前方） |
| 規則3（max z）    | $\arg\max_{j \in \mathcal{L}_k} c_{j,z}$（最大緯度） | $\arg\min_{j} c_{j,z}$（最小緯度） |

### 2.5 評価指標

- **総 $(C_1, C_2)$ ペア数**：$C_1$（出発面）× $C_1$ の隣接面数の総和
- **完全成功数**：全面を剥き終えた（`Length[order] == nFaces`）ペア数
- **ユニーク成功数**：異なる剥き順序（`order` リスト）を持つ成功ペアの数

> ユニーク成功数 = 完全成功数 は，全成功ケースで剥き順が異なることを意味する．

### 2.6 対象多面体

Mathematica の `PolyhedronData` から5種の正多面体（Platonic 立体）のデータを取得した．
面データは `PolyhedronData[name]` の `GraphicsComplex` から抽出した頂点インデックスリストを使用している．

### 2.7 擬似コード

以下に規則2・規則3に共通する外枠アルゴリズムと，各規則固有の選択ステップを示す．

---

**アルゴリズム 1：共通の外枠**

```
入力：多面体データ (vers, faces)，出発面 C₁，第2面 C₂，選択規則 rule ∈ {2, 3}
出力：剥き順序リスト order，完全成功フラグ success

1. vers ← vers − centroid(vers)                     // 重心を原点に移動
2. vers ← RotateToZ(vers, faces, C₁)                // C₁ の重心を +z 軸に整列
3. cc[i] ← mean(vers[faces[i]]) for all i           // 各面の重心を計算
4. adj ← BuildAdjacency(faces)                      // 辺共有に基づく隣接リスト
5. order ← [C₁, C₂]
6. inOrder ← {C₁, C₂}
7. nbrs[i] ← adj[i] \ {C₁, C₂} for all i          // 訪問済みを除去
8. prev ← C₁，last ← C₂

9. while |order| < |faces|:
10.    pending ← nbrs[last] \ inOrder               // 未訪問の隣接面
11.    if pending = ∅: break                         // 行き詰まり → 失敗
12.    next ← SelectNext(cc, prev, last, pending, rule)  // 規則に応じて選択
13.    order.append(next)
14.    inOrder ← inOrder ∪ {next}
15.    nbrs[i] ← nbrs[i] \ {next} for all i
16.    prev ← last，last ← next

17. success ← (|order| = |faces|)
18. return (order, success)
```

---

**アルゴリズム 2：SelectNext（規則2 — loxodrome / 最小角度）**

```
入力：重心配列 cc，直前面 prev，現在面 last，候補集合 pending
出力：次の面 next

1. // Darboux フレームを構築
2. n̂ ← Normalize(cc[last])
3. d ← cc[last] − cc[prev]
4. f̂ ← Normalize(d − (d · n̂) n̂)               // 前進方向（接平面投影）
5. L̂ ← n̂ × f̂                                  // 左方向

6. // 左半空間フィルタ（行列式条件）
7. L ← { j ∈ pending | det(cc[prev], cc[last], cc[j]) ≥ 0 }

8. // 角度計算補助関数
9. φ(j) ← atan2( Proj(cc[j], n̂) · L̂,  Proj(cc[j], n̂) · f̂ )
           ただし Proj(v, n̂) = v − (v · n̂) n̂ を正規化したもの

10. if L ≠ ∅:
11.    next ← argmin_{j ∈ L} φ(j)              // 規則2：左候補から最小角度
12. else:
13.    next ← argmin_{j ∈ pending} |φ(j)|      // フォールバック：最も前方

14. return next
```

---

**アルゴリズム 3：SelectNext（規則3 — max z / 緯度優先）**

```
入力：重心配列 cc，直前面 prev，現在面 last，候補集合 pending
出力：次の面 next

1. // Darboux フレームの法線（左条件の計算に使用）
2. （n̂, f̂, L̂ は規則2と同じ手順で計算）

3. // 左半空間フィルタ（行列式条件）
4. L ← { j ∈ pending | det(cc[prev], cc[last], cc[j]) ≥ 0 }

5. if L ≠ ∅:
6.    next ← argmax_{j ∈ L} cc[j][z]           // 規則3：左候補から最大 z
7. else:
8.    next ← argmin_{j ∈ pending} cc[j][z]     // フォールバック：最小 z

9. return next
```

---

**規則2 と規則3 の差異（SelectNext の比較）**

| ステップ | 規則2（loxodrome） | 規則3（max z） |
|----------|-------------------|---------------|
| 左候補フィルタ | $\det(\mathbf{c}_{k-1}, \mathbf{c}_k, \mathbf{c}_j) \ge 0$ | 同左（共通） |
| 選択基準 | $\arg\min_{j \in \mathcal{L}} \varphi_j$（前進方向に最も近い左旋回） | $\arg\max_{j \in \mathcal{L}} c_{j,z}$（最も高い緯度） |
| フォールバック | $\arg\min_{j \in \text{pending}} |\varphi_j|$（最も前方） | $\arg\min_{j \in \text{pending}} c_{j,z}$（最も低い緯度） |
| 幾何的意味 | 球面上の等角螺旋（loxodrome）の近似 | 緯度ごとに一巡してから次の緯度へ降下 |

---

## 3. 計算結果

### 3.1 比較表

| 多面体 | 面数 | 総 $(C_1,C_2)$ | 成功（規則2） | 成功（規則3） |
|--------|:----:|:--------------:|:------------:|:------------:|
| Tetrahedron（正四面体） | 4 | 12 | **12** (100%) | **12** (100%) |
| Cube（正六面体）        | 6 | 24 | **4** (17%)   | **24** (100%) |
| Octahedron（正八面体）  | 8 | 24 | **24** (100%) | **24** (100%) |
| Dodecahedron（正十二面体） | 12 | 60 | **0** (0%) | **32** (53%) |
| Icosahedron（正二十面体）  | 20 | 60 | **60** (100%) | **60** (100%) |

※「成功」はユニーク成功数．成功数 = ユニーク成功数が全ケースで成立した．

### 3.2 数値的警告（Cube）

正六面体の規則2計算において，以下の警告が出た：

```
Det::luc: Result for Det of badly conditioned matrix ... may contain significant numerical errors.
```

正六面体の面重心は $\{(\pm 0.5, 0, 0),\, (0, \pm 0.5, 0),\, (0, 0, \pm 0.5)\}$
（各軸上）であり，3点が共面（行列式 $\approx 0$）になるケースが多く，
左右判定が数値誤差に依存している可能性がある．

---

## 4. 考察

### 4.1 全体的な傾向

規則3（max z）は規則2（loxodrome）より成功率が高く，
5種の正多面体すべてで **規則3 ≥ 規則2** が成立した．

### 4.2 各多面体の詳細

**正四面体・正八面体・正二十面体（三角形面のみ）**
両規則とも100%成功．これらは三角形面のみからなる多面体（deltahedral）であり，
局所的な幾何が単純なため，どちらの規則でも詰まらない．

**正六面体（正方形面）**
規則2は4/24（17%），規則3は24/24（100%）と大きな差が生じた．
正六面体の面重心が座標軸上に並ぶという高対称性により，
行列式条件で退化（共面）が多発し，規則2の angle 計算が不安定になったと考えられる．
規則3は $z$ 値の大小を基準にするため退化の影響を受けにくい．

**正十二面体（五角形面）**
規則2は0/60（完全失敗），規則3は32/60（53%）．
正十二面体は6種の正多面体の中で最も面数に対して連結性が低い（各面5隣接）ため，
貪欲アルゴリズムが詰まりやすい．loxodrome 的な選択は「前方方向への小さな旋回」を
優先するため，局所的な谷（行き詰まり）にはまりやすいと解釈できる．
規則3（緯度優先）でも53%にとどまる点は，このアルゴリズムの限界を示す．

**正二十面体**
両規則とも60/60（100%）の完全成功．
各面の隣接数が3（最小）で連結性が高く，選択肢が少ないぶん
どちらの規則でも詰まりにくいと考えられる．

### 4.3 loxodrome としての規則2の限界

連続的な球面上の loxodrome は「常に子午線に対して一定角度で進む」性質を持ち，
極へ収束する無限螺旋になる．これを離散多面体に直接適用した場合，
前進方向に近い選択を繰り返すことで「向こう側に回り込む」機会を逃し，
残った面に到達できなくなる可能性がある．正十二面体での完全失敗はこの傾向を端的に示している．

### 4.4 まとめと今後の方針

- **規則3（max z）の採用を支持する**：シンプルな定義で成功率が高く，
  数値退化にも頑健であることが3次元実験で確認された
- **規則2は補助的役割に限定する**：規則3が失敗した場合の補完として
  2段階選択（`summary.tex` 表6）に組み込むことは引き続き検討価値がある
- **正十二面体の改善**：1段バックトラック（`peeling1back` の3D版）を
  適用することで32/60 → より高い成功率への改善が期待できる
- **数値安定性**：退化ケース（行列式 $\approx 0$）に対する $\varepsilon$-閾値の導入を検討する

---

## 5. 正十二面体のパターン詳細比較

### 5.1 整列の向きについて

`p3lAlignTopToZ` は出発面 $C_1$ の**面重心**を $+z$ 軸に整列させる．
正十二面体の場合，$C_1$ は正五角形面であり，その5頂点は全て同一の $z$ 値（$z \approx 1.1135$）に並ぶ．

| 確認項目 | 値 |
|----------|-----|
| top面重心の $z$ | 1.1135 |
| 全頂点の $z_{\max}$ | 1.1135（= top面の頂点） |
| 最上部の形状 | **正五角形面が水平に置かれた状態** |

つまり，現在の整列は「面が上」（face-up）であり，最上部は頂点ではなく面（五角形）である．

### 5.2 到達面数の分布（正十二面体，全60ペア）

**規則2（loxodrome）**

| 到達面数 | ペア数 |
|:--------:|:------:|
| 6 | 60 |
| **合計** | **60（完全成功 0/60）** |

**規則3（max z）**

| 到達面数 | ペア数 |
|:--------:|:------:|
| 11 | 28 |
| 12（完全成功） | 32 |
| **合計** | **60（完全成功 32/60）** |

| 指標 | 規則2 | 規則3 |
|------|:-----:|:-----:|
| 完全成功 | 0/60（0%） | 32/60（53%） |
| 平均到達面数 | 6.0 | 11.5 |

### 5.3 最も注目すべき発見：規則2の「6面の壁」

規則2は**全60ペアで例外なく6面目で行き詰まる**．
正十二面体の面数は12なので，ちょうど半分で必ず止まることになる．

これは偶然ではなく，正十二面体の幾何学的構造に起因すると考えられる：
- 正十二面体には**対極面**（上面と下面）の構造がある．面を$z$の高い方から数えると，上半分6面・下半分6面に分かれる
- loxodrome 的な選択（前方に近い小さな左旋回）は上半分を巡回した後，左半空間内に下半分への橋渡しとなる面が存在しなくなる
- すなわち，loxodrome 規則は「赤道を越えられない」

### 5.4 規則3の失敗パターン（28ペア）

規則3で失敗する28ペアはいずれも**11面まで到達して最後の1面に手が届かない**（到達面数 = 11）．
これは「あと1面」という状況で詰まる最悪のケースであり，1段バックトラックで救済できる可能性が高い．

### 5.5 ペアごとの比較

| 状況 | ペア数 |
|------|:------:|
| 両方成功 | 0 |
| 規則2のみ成功 | 0 |
| 規則3のみ成功 | 32 |
| 両方失敗 | 28 |

規則2が成功するペアは1つも存在せず，規則3が成功するペアは規則2では必ず失敗する．
両規則の成功集合は完全に分離している（規則2の成功集合が空集合）．

### 5.6 規則3の成功順序（全32通り）

規則3で得られた全32通りのユニーク剥き順序を示す（面インデックス，`top` → `f2` → … の順）：

| (C1, C2) | 剥き順序 |
|----------|---------|
| (1, 8)  | 1, 8, 9, 10, 11, 12, 4, 5, 2, 6, 7, 3 |
| (1, 9)  | 1, 9, 10, 11, 12, 8, 6, 7, 2, 3, 4, 5 |
| (1, 12) | 1, 12, 8, 9, 10, 11, 4, 5, 2, 6, 7, 3 |
| (3, 2)  | 3, 2, 4, 11, 10, 7, 6, 5, 8, 12, 1, 9 |
| (3, 4)  | 3, 4, 11, 10, 7, 2, 6, 5, 8, 12, 1, 9 |
| (3, 11) | 3, 11, 10, 7, 2, 4, 5, 12, 8, 1, 9, 6 |
| (4, 3)  | 4, 3, 2, 5, 12, 11, 1, 10, 9, 7, 6, 8 |
| (4, 5)  | 4, 5, 12, 11, 3, 2, 6, 8, 9, 1, 10, 7 |
| (4, 11) | 4, 11, 3, 2, 5, 12, 8, 1, 9, 10, 7, 6 |
| (4, 12) | 4, 12, 11, 3, 2, 5, 6, 8, 9, 1, 10, 7 |
| (5, 2)  | 5, 2, 6, 8, 12, 4, 3, 7, 10, 9, 1, 11 |
| (5, 6)  | 5, 6, 8, 12, 4, 2, 3, 7, 10, 9, 1, 11 |
| (5, 8)  | 5, 8, 12, 4, 2, 6, 7, 9, 10, 1, 11, 3 |
| (6, 2)  | 6, 2, 7, 9, 8, 5, 12, 4, 11, 3, 10, 1 |
| (6, 5)  | 6, 5, 2, 7, 9, 8, 1, 12, 11, 4, 3, 10 |
| (7, 2)  | 7, 2, 3, 10, 9, 6, 8, 5, 12, 4, 11, 1 |
| (7, 3)  | 7, 3, 10, 9, 6, 2, 5, 4, 12, 11, 1, 8 |
| (8, 1)  | 8, 1, 12, 5, 6, 9, 10, 11, 3, 4, 2, 7 |
| (8, 5)  | 8, 5, 6, 9, 1, 12, 11, 4, 3, 2, 7, 10 |
| (8, 12) | 8, 12, 5, 6, 9, 1, 10, 11, 3, 4, 2, 7 |
| (9, 7)  | 9, 7, 6, 8, 1, 10, 3, 2, 4, 5, 12, 11 |
| (9, 10) | 9, 10, 7, 6, 8, 1, 11, 3, 4, 2, 5, 12 |
| (10, 3) | 10, 3, 11, 1, 9, 7, 6, 2, 5, 4, 12, 8 |
| (10, 7) | 10, 7, 3, 11, 1, 9, 6, 2, 5, 4, 12, 8 |
| (10, 11)| 10, 11, 1, 9, 7, 3, 2, 4, 5, 12, 8, 6 |
| (11, 1) | 11, 1, 10, 3, 4, 12, 8, 9, 6, 7, 2, 5 |
| (11, 3) | 11, 3, 4, 12, 1, 10, 9, 7, 6, 2, 5, 8 |
| (11, 10)| 11, 10, 3, 4, 12, 1, 8, 9, 6, 7, 2, 5 |
| (12, 4) | 12, 4, 5, 8, 1, 11, 10, 3, 7, 2, 6, 9 |
| (12, 5) | 12, 5, 8, 1, 11, 4, 3, 2, 7, 6, 9, 10 |
| (12, 8) | 12, 8, 1, 11, 4, 5, 2, 6, 7, 9, 10, 3 |
| (12, 11)| 12, 11, 4, 5, 8, 1, 10, 3, 7, 2, 6, 9 |

---

## 6. 使用コード

```mathematica
(* 実行方法 *)
SetDirectory["...260324Peeling4D"];
Get["peeling3DLoxo.m"];
cmp = p3lCompareRules[];
```

主要関数：

| 関数 | 説明 |
|------|------|
| `p3lExtractPolyhedron[name]` | `PolyhedronData` から頂点・面データを抽出 |
| `p3lBuildAdj[faces]` | 辺共有に基づく隣接リスト構築 |
| `p3lAlignTopToZ[vers, faces, top]` | 出発面を $+z$ 方向に整列 |
| `p3lDarbouxFrame[cPrev, cCurr]` | Darboux フレーム $\{\hat{n}, \hat{f}, \hat{L}\}$ の計算 |
| `p3lAngleFromForward[fHat, lHat, cj]` | $\varphi_j$ の計算 |
| `p3lPeelPair[cc, adj, nf, top, f2, rule]` | 1ペアのペーリング実行 |
| `p3lRunAll[vers, faces, rule]` | 全 $(C_1, C_2)$ ペアの実行 |
| `p3lFromBuiltin[name, rule]` | 正多面体名を指定して実行 |
| `p3lCompareRules[]` | 5種の正多面体で規則2・3を比較 |
